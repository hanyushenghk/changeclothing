# Optional: Google Gemini for garment category detection (otherwise deterministic fallback).
GEMINI_API_KEY=
GEMINI_TRYON_MODEL=gemini-1.5-flash

# Optional: Replicate for try-on generation (otherwise placeholder echo of your photo).
REPLICATE_API_TOKEN=
# Override if Replicate publishes a new default version hash for your model.
REPLICATE_TRYON_VERSION=0513734a452173b8173e907e3a59d19a36266e55b48528559432bd21c7d7e985
